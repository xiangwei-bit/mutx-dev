apiVersion: v1
kind: Service
metadata:
  name: {{ include "mutx.fullname" . }}
  labels:
    {{- include "mutx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mutx.selectorLabels" . | nindent 4 }}
