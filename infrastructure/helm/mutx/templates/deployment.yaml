apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mutx.fullname" . }}
  labels:
    {{- include "mutx.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "mutx.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mutx.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "mutx.fullname" . }}-env
            {{- if .Values.secretEnv }}
            - secretRef:
                name: {{ include "mutx.fullname" . }}-env
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
