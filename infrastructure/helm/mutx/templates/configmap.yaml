apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mutx.fullname" . }}-env
  labels:
    {{- include "mutx.labels" . | nindent 4 }}
{{- if .Values.env }}
data:
  {{- toYaml .Values.env | nindent 2 }}
{{- else }}
data: {}
{{- end }}
