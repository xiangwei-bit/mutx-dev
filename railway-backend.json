{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksVersion": "v0.39.0"
  },
  "deploy": {
    "startCommand": "pip install psycopg2-binary alembic && alembic upgrade head && uvicorn src.api.main:app --host 0.0.0.0 --port 8000",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
